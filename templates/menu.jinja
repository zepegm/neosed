<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
           <i class="fa-solid fa-home"></i>
            Home
        </a>           
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                {% if url_for(request.endpoint) == '/listaAlunos' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Lista de Alunos</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../listaAlunos">Lista de Alunos</a>
                </li>
                {% endif %}

                {% if url_for(request.endpoint) == '/notas' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Notas</a>
                </li> 
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../notas">Notas</a>
                </li> 
                {% endif %}

                {% if url_for(request.endpoint) == '/grade' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Grade Horária</a>
                </li> 
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../grade">Grade Horária</a>
                </li> 
                {% endif %}                

                {% if url_for(request.endpoint) == '/declaracoes' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Declaração</a>
                </li>  
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../declaracoes">Declaração</a>
                </li>  
                {% endif %}        

                {% if url_for(request.endpoint) == '/ficha_matricula' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Ficha de Matrícula</a>
                </li>  
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../ficha_matricula">Ficha de Matrícula</a>
                </li>  
                {% endif %}                                            

                {% if url_for(request.endpoint) == '/calendario' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Calendário</a>
                </li>  
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="/calendario">Calendário</a>
                </li> 
                {% endif %}

                {% if url_for(request.endpoint) == '/frequencia' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Frequência</a>
                </li>
                {% else %}
                <!-- 
                <li class="nav-item">
                    <a class="nav-link" href="../frequencia">Frequência</a>
                </li>
                -->
                {% endif %}


                {% if url_for(request.endpoint) == '/ponto' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Livro Ponto</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../ponto">Livro Ponto</a>
                </li>
                {% endif %}

                {% if url_for(request.endpoint) == '/ponto_adm' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Livro Ponto ADM</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../ponto_adm">Livro Ponto ADM</a>
                </li>
                {% endif %}                  


                {% if url_for(request.endpoint) == '/relatorios' %}
                <li class="nav-item">
                    <a class="nav-link active" href="#">Relatórios</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="../relatorios">Relatórios</a>
                </li>
                {% endif %}                 

            </ul>
        </div>
    </div>
</nav>